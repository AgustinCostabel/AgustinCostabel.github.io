background.gif is the ocean image supplied for the portfolio background.
It is currently a single-frame 500x301 GIF, so it behaves visually like a static image.
If you replace it with an animated GIF using the same filename, the page will use it automatically.
